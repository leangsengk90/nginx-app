apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: vs-qa
  namespace: nginx-qa
spec:
  hosts:
  - "nginx-qa.com"
  gateways:
  - gateway-qa
  http:
  - route:
     - destination:
        host: nginx
        subset: qa


