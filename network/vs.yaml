apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: vs-blue
  namespace: nginx-blue
spec:
  hosts:
  - "nginx-qa.com"
  gateways:
  - gateway-blue
  http:
  - route:
     - destination:
        host: nginx
        subset: blue
